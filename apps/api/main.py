from fastapi import APIRouter

router = APIRouter()

# Route POST /users/login/access-token

# Route GET /users/me

# Route GET /frameworks/

# Route GET /{policy_id}/report

# Route GET /{policy_id}/pdf

# Route GET /{policy_id}/status

# Route POST /upload

# Route POST /

# Route GET /